Jeudi 21 septembre 2023

Complication via le terminal :
Depuis le terminal, se placer dans le bon dossier, ici il s'agit de ~/Bureau/Programmation/TP3 - L'objet suite/TP2 Correction/correction/version2_compact
Ensuite, la première fois faire le set up, faire cmke build .

Pour lancer le cmakefile :
Avoir un dossier propre : juste le CMakeLists.txt et un dossier src avec les fichiers d'exercices (ici main.cpp, Notes.txt, VectorD.cpp et Vector.hpp)
Ouvrir VSCode, ouvrir le fichier en se plaçant à la même racine que le CMakeLists.txt
Normalement, il va demander ce que l'on veut utiliser; prenons Clang.


Puis au focntionnement, faire cmake --build . ainsi que ./td3 (qui est l'executable)

Exercice 1 : ok

Exercice 2 :
q1.
public = les methods sont accessibles partout
Contrairement à private = les methods ne sont accessibles qu'en précisant la classe ou elle est déclarée

q2.
Parce que c'est plus simple pour tout le monde : les utilisateurs ne sont pas noyés par plein d'informations etça permet de ne pas dévoile trop de code non plus (genre caché)

q4. C'est bon, c'est private

q5. Pour une méthode avec peu d'instructions, inline sert à remplacer dans le code le résultat de la fonction.
Pour éviter les appels de fonctions toutes petites (et gagner du temps)